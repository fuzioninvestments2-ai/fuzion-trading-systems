# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# Credenciales — NUNCA commitear
.env
config/credentials.yaml

# Datos generados
*.log
*.db
*.sqlite
*.sqlite3
logs/*.log
logs/*.jsonl
models/*.pkl
models/*.joblib
results/*.json
results/*.csv
cache/

# Keep placeholders
!logs/.gitkeep
!models/.gitkeep
!results/.gitkeep

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Testing
.pytest_cache/
.coverage
htmlcov/

# SSID de Pocket Option (sensible, nunca versionar). Uno por bot:
# ssid.txt (comun), ssid_otc.txt, ssid_real.txt
ssid.txt
ssid_*.txt

# Graficos temporales
charts_tmp*.png

# Datasets exportados (historial para la nube) SI se versionan
!datasets/

# Análisis diagnóstico desechable (el backtest permanente vive en bot/)
scratchpad/

# Runtime del sistema de skills (se generan al operar)
memoria/*.db
memoria/*.json
